# 个人物品库存清单 · 全平台正式上架流程与资料清单

> 项目形态：单用户本地/轻量云工具类 App，无付费、无用户社区（UGC）、无定位/通讯录等敏感权限滥用。
> 包名占位：`com.yourcompany.inventory`；App 名称：`个人物品库存清单`。请替换为真实值。
> 适用范围：中国大陆应用市场 + App Store + 微信小程序。
> 时效提示：文中引用 2024 年起强制执行的 **工信部 App 备案**、**苹果隐私清单** 等新规，请优先以各平台后台实时提示为准。

---

## 0. 上线前必做的「共性三件事」（任一平台都躲不掉）

### 0.1 工信部 App 备案（强制，2024 起）
所有在中国大陆分发（含安卓商店、App Store 中国区、小程序）的应用**必须**完成 App 备案。
1. 先有已 **ICP 备案**的域名 + 国内服务器（见 `deploy_guide.md` 第 5 章）。
2. 登录 https://beian.miit.gov.cn → 「APP 备案」→ 填应用名称、包名、域名、服务者资质。
3. 审核约 1~20 个工作日，管局短信核验。**未备案应用商店一律拒收**。

### 0.2 软件著作权（安卓商店硬性门槛）
应用宝/华为/小米/OPPO/vivo 均要求提供**软著登记证书**（或受理通知书，部分市场可「承诺制」先用受理号上架）。
- 自己申请：中国版权保护中心 https://www.ccopyright.com.cn，费用约 0（官费减免），周期 30~60 天。
- 代理加急：市价 600~1500 元，7~20 天下证。**建议项目立项即同步申请**，别等要上架才办。

### 0.3 隐私政策 + 权限说明（三端通审）
- 独立托管一份 `privacy.html`（挂在已备案域名下，如 `https://yourdomain.com/privacy.html`）。
- 内容必须包含：收集的信息项（本 App 仅本地库存数据，无上传则写明「数据仅存储于本机」）、用途、用户权利、联系方式。
- App 首次启动弹窗同意；商店提交时填该 URL。
- 权限最小化：本类工具只需**网络（联网版）**、**存储（读写备份）**，不要申请定位/通讯录/相机等无关权限。

---

## 1. 安卓端：应用宝 / 华为 / 小米 上架

### 1.1 通用打包（Flutter 出包）
```bash
# 生成签名 keystore（仅一次）
keytool -genkey -v -keystore upload-keystore.jks -keyalg RSA -keysize 2048 -validity 10000 -alias upload

# 配置 android/key.properties + build.gradle 签名
# 打正式 Release AAB（应用商店优先 AAB，部分市场仍收 APK）
flutter build appbundle --release     # 产物 build/app/outputs/bundle/release/app-release.aab
flutter build apk --release --split-per-abi   # 兼容收 APK 的市场
```

### 1.2 应用宝（腾讯开放平台 / 应用宝）
- 入口：https://open.qq.com → 「应用接入」→ 移动应用
- 账号资质：**企业/个人实名**（个人可发非特殊类目，但工具类建议企业主体更稳）
- 资料清单：
  | 资料 | 说明 |
  |---|---|
  | 软著证书 | 必备 |
  | 应用图标 512×512、截图 3~5 张（1080×1920） | 无边框、无「Demo」字样 |
  | 应用简介 + 一句话描述 | ≤30 字简介 |
  | 隐私政策 URL | 必须可访问 |
  | 权限列表及说明 | 逐项解释 |
  | App 备案号 | 2024 起必填 |
- 流程：注册→实名→创建应用→填资料传包→自测→提交审核（1~3 工作日）→ 上架。
- 注意：应用宝对「工具类」审核较松，但**闪屏/开屏广告、诱导分享、捆绑下载**会驳回。

### 1.3 华为应用市场
- 入口：https://developer.huawei.com/consumer/cn/ → 华为开发者联盟
- 资质：企业开发者实名（华为对个人主体限制较多，推荐企业）。
- 资料：软著 + 隐私政策 + 应用备案号 + 测试账号（如有登录）+ 版权承诺函（无软著时用）。
- 特有：**「应用安全漏洞扫描」** 华为会扫 APK，需提前用华为提供的 **DevEco 安全检测**或第三方扫描修掉高危漏洞（见 `test_plan.md` 安全章节）。
- 流程：联盟实名→创建应用→上传 AAB/APK→内容审核（含机器+人工，2~5 工作日）→ 发布。

### 1.4 小米应用商店
- 入口：https://dev.mi.com → 小米开放平台
- 资质：个人/企业均可，企业更稳。
- 资料：软著 + 隐私 + 备案号 + 图标/截图 + 应用承诺函。
- 流程：注册→实名→提交应用→审核（1~3 工作日）→ 上架。小米对**权限过度申请**敏感，务必最小集。

### 1.5 三大市场横向对比
| 项 | 应用宝 | 华为 | 小米 |
|---|---|---|---|
| 主体要求 | 个人/企业 | 企业优先 | 个人/企业 |
| 软著 | 必需 | 必需 | 必需 |
| 收包格式 | AAB/APK | AAB 优先 | AAB/APK |
| 审核时长 | 1~3 天 | 2~5 天 | 1~3 天 |
| 常见驳回 | 开屏广告/诱导 | 安全漏洞扫描 | 权限过度 |

---

## 2. iOS 端：App Store 上架

### 2.1 开发者账号注册（$99/年）
1. 访问 https://developer.apple.com → 「Account」→ 注册 Apple ID。
2. 加入 **Apple Developer Program**（个人/公司 $99/年；企业 $299 不需要上架）。
3. 公司主体需 **D-U-N-S 邓白氏编码**（免费申请，1~5 天）。
4. 付款后账号生效，可用于证书签名与 App Store Connect。

### 2.2 证书 / Identifiers / 描述文件（Xcode 自动或手动）
- 推荐用 **Xcode 自动签名**（Target → Signing & Capabilities → 勾选 Automatically manage signing）。
- 手动路径：开发者后台 → Certificates（Apple Distribution）+ Identifiers（Bundle ID = 包名）+ Profiles（App Store 类型）。

### 2.3 App Store Connect 创建应用
1. https://appstoreconnect.apple.com → 「我的 App」→ 新建（填名称、Bundle ID、SKU、主语言 zh-CN）。
2. 填** App 信息**：副标题、类别（选 **「工具 / Utilities」** 或 「效率 / Productivity」）、年龄分级（4+，无不良内容）、定价（免费）。
3. **隐私 nutrition 标签**：在「App 隐私」逐项声明数据收集情况——本 App 本地存储、不上传，则全部选「不收集」。
4. **隐私清单（Privacy Manifest，2024 强制）**：在 Xcode 工程加 `PrivacyInfo.xcprivacy`，声明使用的「需说明原因 API」（如 `UserDefaults`、文件时间戳等）及原因码，否则上架会被拒。

### 2.4 IPA 打包与提交
```bash
# Xcode 归档（GUI：Product → Archive）
flutter build ios --release
# 用 Xcode 打开 ios/Runner.xcworkspace → Product → Archive → Validate → Distribute → App Store Connect
```
或命令行：
```bash
# 导出 IPA 需先 Archive，再用 xcodebuild 导出（需 ExportOptions.plist）
xcodebuild -exportArchive -archivePath build/Runner.xcarchive \
  -exportPath build/ipa -exportOptionsPlist ExportOptions.plist
```

### 2.5 提审流程与 TestFlight
1. 上传 IPA 后，在 App Store Connect 选构建版本 → 填**审核信息**（联系方式、备注、演示账号若无登录则填「无登录功能」）。
2. **TestFlight** 内部测试（无需审核即可发本团队）先自测一遍。
3. 提交「送审」→ 等待（通常 24~48 小时，首次更久）。
4. 过审后手动「发布」或定时发布。

### 2.6 iOS 注意事项（易拒点）
- **隐私清单缺失**（2024 新规，必拒）。
- **隐私标签与实际不符**：声明不收集却调用网络上传会被拒。
- **崩溃/空白页**：送审前真机跑通核心流程（本 App：新增/用掉1/看板）。
- **中国区特殊**：含用户内容、新闻、医疗等功能需额外许可；本工具不涉及。
- **元数据真实**：截图不得含虚假功能、不得用「免费」诱导但实际内购（本 App 无内购）。

---

## 3. 微信小程序上架

### 3.1 类目选择（最关键一步）
- 入口：https://mp.weixin.qq.com → 注册小程序 → 「设置 → 基本设置 → 服务类目」。
- 本项目建议类目：**「工具」→「备忘录 / 记事 / 效率」** 或 **「生活服务」→「物品管理」**（以后台可选项为准）。
- 工具类目**通常无需额外资质**，但需提交《小程序隐私保护指引》。
- ⚠️ 不要选错成「电商/医疗/政务」等需特许资质的类目，会卡审核。

### 3.2 服务器域名校验（联网版必需）
小程序**只可请求 HTTPS 域名**，且域名须 **ICP 备案**：
1. 后台「开发 → 开发管理 → 开发设置 → 服务器域名」。
2. 配置：
   - **request 合法域名**：`https://yourdomain.com`（后端 API）
   - **uploadFile / downloadFile 合法域名**：同上（备份/同步用）
   - **业务域名**：若内嵌网页需填并下载校验文件放服务器根目录
3. 本地开发可在「开发工具」勾「不校验合法域名」调试，但**提审必须配真域名**。

### 3.3 隐私协议与备案
- 配置《小程序隐私保护指引》：声明收集的数据（本地优先则写「本地存储」）。
- **小程序备案**：2023 起微信小程序也需工信部 App 备案（同第 0.1 章，走微信侧备案通道）。
- 调用 `wx.getUserProfile` 等需在隐私协议勾选对应接口并弹窗授权。

### 3.4 提交审核流程
1. 微信开发者工具 → 上传代码（填版本号、备注）。
2. 后台「管理 → 版本管理 → 提交审核」。
3. 填**审核信息**：功能页路径（如 `pages/dashboard/dashboard`）、测试账号（无则填「无需登录」）、类目。
4. 微信官方审核（通常 1~3 工作日，首次偏慢）。
5. 过审后「发布」即上线，用户搜名或扫码即用。

### 3.5 小程序注意事项
- **虚拟支付**：小程序个人主体不能做虚拟支付；本 App 免费无影响。
- **诱导分享**：不得强制分享才能用功能，否则驳回。
- **内容安全**：若后续加 UGC 需接微信内容安全 API；当前纯本地无此要求。
- **备案未完成前可开发调试，但无法正式发布**。

---

## 4. 资料清单总表（一次备齐）

| 资料 | 安卓 | iOS | 小程序 |
|---|---|---|---|
| 软著证书 | ✅ | ❌ | ❌ |
| 工信部 App 备案号 | ✅ | ✅ | ✅ |
| 隐私政策 URL | ✅ | ✅ | ✅(隐私指引) |
| 企业/个人实名资质 | ✅ | ✅($99) | ✅ |
| 图标 512/1024 | ✅ | ✅(1024) | ✅(不同尺寸) |
| 截图/演示 | ✅ | ✅ | ✅ |
| HTTPS 已备案域名 | 联网版 | 联网版 | ✅必需 |
| 测试账号/说明 | 按需 | 按需 | 按需 |
| 隐私清单 | ❌ | ✅(iOS 专属) | ❌ |

---

## 5. 时间线与常见驳回（避坑）

**预估周期**（并行办理可缩短）：
- 软著申请：30~60 天（加急 7~20 天）——**最早启动**
- 域名 ICP 备案：3~20 天
- App 备案：1~20 天
- 各商店审核：1~5 天/平台
- iOS 首审：2~7 天

**高频驳回原因**：
1. 软著/备案缺失 → 直接退。
2. 隐私政策打不开或为空。
3. 权限过度（申请了用不到的权限）。
4. iOS 缺隐私清单 / 隐私标签不实。
5. 小程序域名未备案或 HTTP。
6. 应用内有明显 Bug、闪退、空白页。

> 建议顺序：**先软著 + 域名备案并行 → 后端部署 + HTTPS → iOS 隐私清单 + 安卓安全扫描 → 三端同步提交**。
