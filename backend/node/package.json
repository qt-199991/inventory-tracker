{
  "name": "inventory-backend-node",
  "version": "1.2.0",
  "type": "module",
  "private": true,
  "description": "个人物品库存清单 — 多设备共享后端（零依赖 Node，嵌入式 JSON 数据库）",
  "scripts": {
    "start": "node server.mjs",
    "test": "node --test tests/*.test.mjs"
  },
  "engines": {
    "node": ">=18"
  }
}
