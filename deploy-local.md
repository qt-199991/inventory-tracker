# 本机稳定运行部署指南（V1.1）

库存清单是一个**纯前端单文件应用**，数据存在浏览器本地（IndexedDB / localStorage 兜底）。
下面三种方式都能用，按省事程度排序。

---

## 方式一：双击打开（最简单，已经能用）

直接双击 `inventory.html`，用系统默认浏览器打开即可。

- ✅ 零配置，点开就用。
- ⚠️ 注意：少数浏览器在 `file://` 协议下会限制本地存储，可能出现「刷新后数据丢失」。
  应用已内置 IndexedDB → localStorage 自动降级，绝大多数情况可用；若你发现数据不持久，请改用方式二。

---

## 方式二：本机起一个本地服务器（推荐，数据最稳）

在**本机**打开 PowerShell 或命令提示符，进入 `inventory.html` 所在目录，执行下面任一命令：

```powershell
# 方式 A：本机有 Python（推荐）
python -m http.server 8000

# 方式 B：本机有 Node.js
npx serve -l 8000 .
```

然后浏览器访问：`http://localhost:8000/inventory.html`

- 已为你准备好**一键脚本**：双击桌面上的 `run-local-server.bat` 即可自动起服务（自动选 Python 或 Node）。
- 关闭那个命令行窗口 = 停止服务。
- `http://localhost` 是安全的同源环境，IndexedDB 完整可用，数据最持久。

---

## 方式三：生产级部署（多人 / 长期 / 多端）

适合「安卓 + iOS + 小程序 + 多用户云端同步」的场景，详见 `deploy_guide.md`：

- SpringBoot 3.2 后端 + MySQL 8.0（用户表 / 物品表 / 批次表）
- Nginx 反代 + Let's Encrypt 免费 HTTPS
- 域名备案 + 软著（上架应用市场必备）
- 服务器 JDK 安装、MySQL 远程授权、防火墙、日志查看、备份脚本全套指令

---

## 数据备份与恢复（重要）

应用内点右上角 **⚙（齿轮）→ 导出备份**，会下载一个 `inventory-backup-日期.json`。
- 换手机 / 重装系统 / 清浏览器前，先导出。
- 到新环境后，点 **⚙ → 导入备份** 选这个 JSON 即可恢复全部物品和账号。

---

## 常见问题

| 现象 | 原因 | 解决 |
|---|---|---|
| 双击 `inventory.html` 后刷新数据没了 | 部分浏览器 `file://` 限制存储 | 用方式二起本地服务器 |
| 浏览器访问 `localhost:8000` 打不开 | 服务没起 / 端口被占 | 确认 bat 或命令已运行；换端口如 8080 |
| 照片不显示 | 误删了 base64 数据 | 编辑该物品重选照片 |
| 导入提示格式不正确 | 选错文件 | 只选本应用导出的 JSON |
